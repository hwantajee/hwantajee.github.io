const btnScr = document.getElementsByTagName('button'); 

for(let i = 0; i < btnScr.length; i++){ 
    btnScr[i].addEventListener('click',function(e){ 
        e.preventDefault(); 
        document.querySelector('button' + (i + 1)).scrollIntoView(true);
     }); 
}